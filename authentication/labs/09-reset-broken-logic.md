## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** استغلال خلل في منطق إعادة تعيين كلمة المرور لتغيير كلمة مرور ضحية.
- **الهدف النهائي:** تسجيل الدخول لحساب `carlos`.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Logic Flaw / Lack of Verification
- **الوصف:** السيرفر لا يتحقق من مطابقة توكن إعادة التعيين (`temp-forgot-password-token`) لاسم المستخدم المرسل في نفس الطلب. يمكن للمهاجم طلب توكن لنفسه، ثم استخدامه في طلب آخر مع تغيير اسم المستخدم إلى اسم الضحية.

## 3. خطوات الحل (Solution Steps)

1. **طلب التوكن:** طلب "Forgot password" لحسابك الخاص (`wiener`).
2. **الالتقاط:** الحصول على التوكن من الإيميل أو عبر اعتراض الطلب.
3. **التلاعب:** إرسال طلب تغيير الباسورد باستخدام التوكن الخاص بك، مع تغيير حقل `username=wiener` إلى `username=carlos`.
4. **الاستغلال:** تسجيل الدخول بكلمة المرور الجديدة لحساب `carlos`.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Suite (Proxy Intercept, Repeater)

## 5. لقطات الشاشة (Screenshots)

>
> [!IMPORTANT]
> الخلل يكمن في عدم وجود ربط (Mapping) في قاعدة البيانات بين التوكن وحساب المستخدم صاحب الطلب.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Token-User Binding:** يجب أن يتم توليد التوكن وربطه فريداً بمعرف المستخدم في السيرفر.
- **Verification:** التحقق من أن التوكن المستخدم ينتمي فعلياً للمستخدم الذي يحاول تغيير كلمة مروره.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/authentication/other-mechanisms/lab-password-reset-broken-logic)
