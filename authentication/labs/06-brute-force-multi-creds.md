## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز حماية الـ Brute-force عبر إرسال عدة محاولات في طلب واحد.
- **الهدف النهائي:** تسجيل الدخول لحساب `carlos`.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Logic Flaw / Parameter Manipulation
- **الوصف:** الخادم يقبل مصفوفة (Array) من كلمات المرور في حقل الـ JSON بدلاً من نص مفرد. يقوم الخادم بالتحقق من كل عناصر المصفوفة في دورة برمجية واحدة، مما يسمح بتجربة مئات الكلمات في طلب HTTP واحد وتجنب عدّادات الحماية التي تراقب عدد الطلبات.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** ملاحظة أن الطلب يستخدم JSON: `{"username":"carlos", "password":"..."}`.
2. **التلاعب:** تحويل حقل `password` إلى مصفوفة: `{"username":"carlos", "password":["p1", "p2", "p3", ...]}`.
3. **التشغيل:** إرسال طلب واحد يحتوي على قائمة كلمات المرور كاملة.
4. **التحقق:** البحث عن استجابة `302 Found` التي تحتوي على كوكي جلسة صالح.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Suite (Repeater)
- [x] Python Script (لتحويل قائمة الكلمات إلى مصفوفة JSON)

## 5. لقطات الشاشة (Screenshots)

>
> [!TIP]
> تذكر دائماً فحص ما إذا كان المدخل يقبل أنواع بيانات مختلفة (Arrays/Objects).

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Strong Typing:** تأكد من أن المدخلات تلتزم بنوع بيانات محدد (String فقط) ولا تقبل مصفوفات إلا إذا كان ذلك مقصوداً.
- **Internal Loop Rate Limiting:** تطبيق حماية داخلية تمنع تجربة أكثر من كلمة مرور واحدة لكل عملية مصادقة.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/authentication/password-based/lab-broken-brute-force-protection-multiple-credentials-per-request)
