## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** التلاعب بخاصية الـ `href` الخاصة برابط باستخدام مكتبة jQuery.
- **الهدف النهائي:** تنفيذ جافاسكريبت عند الضغط على رابط "Back".

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** DOM-based XSS (Attribute Sink)
- **الوصف:** التطبيق يستخدم jQuery لتحديث رابط العودة (`#backLink`) عبر دالة `.attr('href', returnUrl)`. القيمة تأتي من بارامتر `returnUrl` في الرابط. بما أن المتصفح يدعم بروتوكول `javascript:` داخل الـ `href`ـ يمكن للمهاجم حقن كود برمجى بدلاً من رابط حقيقي.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** فحص صفحة "Submit Feedback" وملاحظة الكود الذي يحدث الرابط.
2. **التلاعب بالـ URL:** إضافة البايلود لبارامتر الرجوع:

   ```
   ?returnUrl=javascript:alert(1)
   ```

3. **التنفيذ:** الضغط على رابط "Back" في أسفل الصفحة لتشغيل الكود.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (URL Manipulation)
- [x] jQuery Source Analysis

## 5. لقطات الشاشة (Screenshots)

>
> [!IMPORTANT]
> أي مكان يقبل رابطاً (URL) هو مرشح لثغرة `javascript:` protocol injection.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Protocol Whitelisting:** تأكد من أن الرابط يبدأ بـ `http://` أو `https://` فقط قبل استخدامه.
- **Relative Paths:** استعمل مسارات نسبية (Relative paths) إذا أمكن لمنع حقن بروتوكولات خارجية.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-jquery-href-attribute-sink)
