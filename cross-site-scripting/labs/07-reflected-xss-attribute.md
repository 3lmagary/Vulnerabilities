## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز فلترة الأقواس (`<>`) باستخدام سياق الخصائص (Attributes).
- **الهدف النهائي:** تنفيذ `alert()` عند تفاعل المستخدم مع عنصر الإدخال.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Reflected XSS (Attribute Context)
- **الوصف:** التطبيق يمنع حقن التاجات عبر ترميز `<` و `>`، ولكن الإدخال يظهر داخل قيمة خاصية `value` في وسم `<input>`. بما أن علامات التنصيص (`"`) غير مرمزة، يمكن للمهاجم "كسر" الخاصية وإضافة خصائص جديدة (Events) للعنصر نفسه.

## 3. خطوات الحل (Solution Steps)

1. **الاكتشاف:** إدخال نص يحتوي على `"` وملاحظة كسر هيكلية الوسم في كود المصدر.
2. **الحقن:** إغلاق الخاصية الحالية وإضافة حدث `onmouseover` (أو أي حدث آخر):

   ```
   " onmouseover="alert(1)
   ```

3. **التنفيذ:** عند تمرير الماوس فوق خانة البحث في الصفحة الناتجة، سيتم تنفيذ السكربت.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (Source Code Inspection)

## 5. لقطات الشاشة (Screenshots)

![Attribute Injection](../assets/Screenshot-From-2025-10-17-15-45-43.png)

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Attribute Encoding:** يجب ترميز علامات التنصيص المفردة والمزدوجة عند وضع البيانات داخل الخصائص.
- **HTML Entity Encoding:** تحويل كل الرموز غير النصية إلى كيانات HTML.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/contexts/lab-attribute-angle-brackets-html-encoded)
