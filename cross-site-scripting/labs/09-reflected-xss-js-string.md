## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** كسر سلسلة نصية داخل كود جافاسكريبت (JavaScript String Breaking).
- **الهدف النهائي:** تنفيذ `alert()` رغم ترميز الأقواس (`<>`).

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Reflected XSS (JS String Context)
- **الوصف:** الإدخال يظهر داخل متغير جافاسكريبت: `var searchTerms = 'USER_INPUT';`. السيرفر يرمّز الأقواس، لكنه يسمح بعلامة الاقتباس المفردة (`'`) والشرطة المائلة (`/`). هذا يسمح للمهاجم بإنهاء السلسلة النصية وكتابة أوامر جافاسكريبت جديدة في نفس السطر.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** عرض كود المصدر وملاحظة مكان وجود الكلمة المبحوث عنها.
2. **كسر السياق:** نحتاج لإغلاق المتغير وإضافة أمر جديد ثم تعطيل بقية السطر:

   ```javascript
   '; alert(1)//
   ```

3. **النتيجة:** سيصبح الكود في المتصفح: `var searchTerms = ''; alert(1)//';` وهو كود سليم برمجياً سيؤدي لتنفيذ التنبيه.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (URL Manipulation)

## 5. لقطات الشاشة (Screenshots)

![JS String Break](assets/Screenshot From 2025-11-18 20-02-01.png)

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **JS Escaping:** استخدام الـ Backslash للهروب من علامات الاقتباس (مثلاً `'` تصبح `\'`).
- **JSON Encoding:** تمرير البيانات عبر `JSON.stringify` لضمان صحة السلاسل النصية وأمانها.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/contexts/lab-javascript-string-angle-brackets-html-encoded)
