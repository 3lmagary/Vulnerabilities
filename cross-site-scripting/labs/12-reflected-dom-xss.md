## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** استغلال ثغرة Reflected DOM XSS باستخدام دالة `eval()`.
- **الهدف النهائي:** تجاوز الـ Backslash Escaping لتنفيذ `alert()`.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Reflected DOM-based XSS (eval Sink)
- **الوصف:** التطبيق يقوم بطلب بيانات عبر AJAX ثم يمرر الاستجابة (JSON) مباشرة لدالة `eval()`. السيرفر يحاول منع الخروج من الـ JSON String بإضافة `\` قبل علامات التنصيص. ومع ذلك، يمكننا إرسال `\` إضافية من العميل لإلغاء مفعول الـ Escape الخاص بالسيرفر.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** الكود الضعيف هو: `eval('var results = ' + responseText);`.
2. **الاكتشاف:** السيرفر يرد بـ `{"searchTerm":"USER_INPUT"}`.
3. **الاستغلال (Double Escape):** إرسال البايلود التالي:

   ```javascript
   \"-alert(1)}//
   ```

   سيقوم السيرفر بتحويله إلى `\\"-alert(1)}//` داخل الـ JSON. عند معالجته بـ `eval`ـ الـ `\\` ستصبح `\` واحدة حرفية، والـ `"` ستصبح غير محمية، مما يكسر الـ String وينفذ السكربت.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Suite (Interception/Repeater)
- [x] Browser Console

## 5. لقطات الشاشة (Screenshots)

>
> [!CAUTION]
> دالة `eval()` هي أحد أخطر الـ Sinks في جافاسكريبت لأنها تحول أي نص إلى كود قابل للتنفيذ.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Avoid eval():** استخدم `JSON.parse()` بدلاً من `eval()` لمعالجة بيانات الـ JSON؛ فهي آمنة تماماً ضد تنفيذ الأكواد.
- **Secure Coding:** لا تثق أبداً في البيانات القادمة من السيرفر إذا كانت قد مرت عبر مدخلات مستخدم سابقاً.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-dom-xss-reflected)
