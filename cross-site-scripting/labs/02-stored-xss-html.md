## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** زرع كود خبيث في قاعدة بيانات الموقع ليتم تنفيذه عند كل زائر.
- **الهدف النهائي:** تنفيذ دالة `alert()` بشكل دائم في صفحة المقال.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Stored XSS
- **الوصف:** الثغرة تكمن في نظام التعليقات؛ حيث يتم تخزين نص التعليق في قاعدة البيانات وعرضه لاحقاً لأي زائر دون ترميز. هذا يسمح للمهاجم بحقن سكربت يتم تنفيذه "تلقائياً" لكل من يفتح الصفحة.

## 3. خطوات الحل (Solution Steps)

1. **الاكتشاف:** كتابة تعليق يحتوي على وسم HTML بسيط مثل `<u>Test</u>` وملاحظة إذا كان النص سيظهر مسطراً (ما يعني قبول التاج).
2. **الاستغلال:** إرسال تعليق يحتوي على البايلود:

   ```html
   <script>alert(1)</script>
   ```

3. **التأكيد:** فتح صفحة المقال، وسيظهر التنبيه مباشرة عند التحميل.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (Comment form)

## 5. لقطات الشاشة (Screenshots)

>
> [!IMPORTANT]
> الـ Stored XSS أخطر بمراحل من الـ Reflected لأنه لا يتطلب إرسال رابط ملغوم للضحية.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Input Sanitization:** استخدام مكتبات موثوقة (مثل DOMPurify) لتنظيف مدخلات المستخدم قبل تخزينها.
- **Context-Aware Encoding:** ترميز البيانات بناءً على المكان الذي ستعرض فيه.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/stored/lab-html-context-nothing-encoded)
