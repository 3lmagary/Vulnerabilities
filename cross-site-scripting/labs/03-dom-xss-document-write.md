## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** استغلال سوء استخدام دالة `document.write` في جانب العميل (DOM).
- **الهدف النهائي:** تنفيذ JavaScript عبر التلاعب بالـ URL.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** DOM-based XSS
- **الوصف:** المشكلة تكمن في كود جافاسكريبت داخل الصفحة يأخذ قيمة من الـ URL (`location.search`) ويقوم بكتابتها مباشرة في الصفحة باستخدام `document.write()`. المتصفح سيقوم بتفسير أي HTML محقون كجزء من بنية الصفحة.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** فحص السكربتات في الصفحة والبحث عن `document.write`.
2. **الاكتشاف:** ملاحظة أن محرك البحث يستخدم السكربت لكتابة نص البحث داخل الصفحة.
3. **الحقن:** بما أن الحقن يتم داخل سياق HTML، يجب أولاً إغلاق أي تاجات مفتوحة (إن وجدت) ثم وضع السكربت:

   ```html
   "><script>alert(1)</script>
   ```

4. **التنفيذ:** بمجرد تحميل الرابط مع البايلود، سيتم تنفيذ السكربت.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser Developer Tools (Debugger/Sources)

## 5. لقطات الشاشة (Screenshots)

>
> [!NOTE]
> ثغرات الـ DOM XSS تحدث بالكامل في المتصفح، وقد لا تصل البيانات "الملغومة" للسيرفر أصلاً.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Safe Sinks:** تجنب استخدام `document.write()` واستبدلها بـ `textContent`.
- **Logic Review:** تأكد من أن أي بيانات من مصادر غير موثوقة (Sources) يتم معالجتها بحذر قبل تمريرها للـ Sinks.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-document-write-sink)
