## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز ترميز الرموز الخاصة عبر استغلال تعبيرات (Expressions) AngularJS.
- **الهدف النهائي:** تنفيذ `alert(1)` رغم ترميز الأقواس والاقتباسات.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** DOM-based XSS (AngularJS Injection)
- **الوصف:** التطبيق يستخدم مكتبة AngularJS، وبما أنه يقوم بعرض مدخلات المستخدم داخل وسم يحتوي على `ng-app`ـ فإن هذه المدخلات سيتم تقييمها كتعبيرات AngularJS (Expressions). هذا يسمح للمهاجم باستخدام الـ Double Curly Braces `{{}}` لتنفيذ كود جافاسكريبت داخل بيئة "Sandbox" الخاصة بـ AngularJS.

## 3. خطوات الحل (Solution Steps)

1. **الاكتشاف:** إدخال `{{7*7}}` وملاحظة ظهور النتيجة `49` في الصفحة.
2. **الاستغلال (Sandbox Escape):** بما أن الأقواس العادية محظورة، نستخدم الـ `constructor` للوصول لبيئة التنفيذ العامة:

   ```javascript
   {{$on.constructor('alert(1)')()}}
   ```

3. **التنفيذ:** بمجرد تحميل الصفحة، سيقوم AngularJS بتنفيذ التعبير وإنشاء دالة جافاسكريبت تنفذ التنبيه.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (AngularJS Expression Testing)

## 5. لقطات الشاشة (Screenshots)

>
> [!IMPORTANT]
> ثغرات الـ Client-side frameworks مثل Angular و Vue تتطلب فهم "البيئة" الخاصة بها لتجاوز قيود الـ XSS التقليدية.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Prevent User Input in ng-app:** لا تضع مدخلات المستخدم أبداً داخل عناصر تخضع لمعالجة AngularJS.
- **Use ng-non-bindable:** استخدم هذه الخاصية لتعطيل تقييم التعبيرات في المساحات التي تحتوي على بيانات من المستخدم.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-angularjs-expression)
