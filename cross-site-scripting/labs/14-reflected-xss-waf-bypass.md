## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز جدار الحماية (WAF) عبر استخدام تاجات وخصائص HTML نادرة.
- **الهدف النهائي:** تنفيذ `print()` تلقائياً للضحية دون تفاعل.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Reflected XSS (WAF Bypass)
- **الوصف:** يوجد WAF يحظر التاجات الشائعة (`script`, `img`, `svg`). بعد البحث (Fuzzing)، وجدنا أن وسم `<body>` وحدث `onresize` مسموح بهما. التحدي هو أن `onresize` يتطلب تغيير حجم الصفحة؛ لذا سنستخدم هجوماً خارجياً لمحاكاة ذلك.

## 3. خطوات الحل (Solution Steps)

1. **الاكتشاف (Fuzzing):** استخدام Burp Intruder لتجربة كل التاجات والخصائص لمعرفة المسموح به.
2. **تجهيز الهجوم:** استخدام Exploit Server لاستضافة `iframe` يقوم بتصغير/تكبير الموقع المصاب تلقائياً:

   ```html
   <iframe src="https://YOUR-LAB-ID.web-security-academy.net/?search=<body onresize=print()>" 
           width="100px" 
           onload="this.style.width='500px'">
   </iframe>
   ```

3. **التنفيذ:** إرسال الهجوم للضحية لتشغيل الثغرة.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Intruder (For Tag/Attribute Fuzzing)
- [x] Exploit Server

## 5. لقطات الشاشة (Screenshots)

>
> [!TIP]
> عندما يتم حظر كل شيء، ابحث عن الـ "Corner Cases" أو التاجات المهملة في الـ HTML Specification.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Allowlisting:** بدلاً من حظر التاجات السيئة، اسمح فقط بالتاجات والخصائص الآمنة والمحددة مسبقاً.
- **Strict WAF Rules:** تحديث قواعد الـ WAF لتشمل كافة الأحداث (Events) التي قد تؤدي لتنفيذ جافاسكريبت.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/contexts/lab-html-context-with-most-tags-and-attributes-blocked)
