## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز وظيفة تنظيف HTML غير مكتملة (Single-replacement bypass).
- **الهدف النهائي:** تنفيذ Stored XSS عبر تكرار التاجات.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Stored DOM-based XSS
- **الوصف:** التطبيق يستخدم دالة `replace()` لتنظيف التاجات (`<` و `>`). المشكلة هي أن دالة `replace` في جافاسكريبت (الحالة الافتراضية) تقوم باستبدال **أول ظهور فقط** للرمز. إذا قام المستخدم بتكرار الأقواس، سيتم تنظيف الأولى وترك البقية كما هي.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** فحص دالة الـ Sanitization في ملفات الـ JS: `html.replace('<', '&lt;').replace('>', '&gt;')`.
2. **الاستغلال:** نضع "طُعماً" في البداية ليتم تنظيفه، ونضع البايلود الحقيقي بعده:

   ```html
   <><img src=x onerror=alert(1)>
   ```

3. **التنفيذ:** سيتحول النص إلى `&lt;&gt;<img src=x onerror=alert(1)>`؛ المتصفح سيعرض الجزء الأول كنص، وينفذ الجزء الثاني ككود HTML.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser Developer Tools (Source analysis)

## 5. لقطات الشاشة (Screenshots)

>
> [!IMPORTANT]
> لعمل استبدال شامل في JS، يجب استخدام Regular Expression مع علم الـ Global (`/g`) أو استخدام `.replaceAll()`.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Global Replacement:** تأكد من استبدال جميع حالات الرموز الخطيرة.
- **Standard Libraries:** لا تكتب دوال التنظيف بنفسك؛ استخدم مكتبات مجربة مثل `DOMPurify`.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-dom-xss-stored)
