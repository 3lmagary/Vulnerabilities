## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** اكتشاف وتنفيذ ثغرة Reflected XSS أساسية في سياق HTML.
- **الهدف النهائي:** تنفيذ دالة `alert()` في متصفح المستخدم.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Reflected XSS (Basic HTML Context)
- **الوصف:** التطبيق يقبل مدخلات من المستخدم (مثل استعلام البحث) ويعرضها مباشرة داخل الصفحة دون إجراء أي عملية تنقية (Sanitization) أو ترميز (Encoding) للرموز الخاصة بـ HTML مثل `<` و `>`.

## 3. خطوات الحل (Solution Steps)

1. **الاختبار:** إدخال نص بسيط وملاحظة انعكاسه في الصفحة.
2. **الحقن:** بما أن التاجات يتم قبولها، استخدم البايلود التالي في خانة البحث:

   ```html
   <script>alert(1)</script>
   ```

3. **التنفيذ:** بمجرد الضغط على بحث، سيقوم المتصفح بتفسير النص ككود جافاسكريبت وتنفيذه.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (Direct Injection)

## 5. لقطات الشاشة (Screenshots)

>
> [!TIP]
> دائماً ابدأ بأبسط بايلود ممكن للتأكد من عدم وجود فلاتر أو WAF.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Output Encoding:** تحويل الرموز الخاصة إلى HTML entities (مثلاً `<` تصبح `&lt;`).
- **CSP:** تفعيل سياسة أمان المحتوى لمنع تنفيذ السكربتات المضمنة (Inline Scripts).

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/reflected/lab-html-context-nothing-encoded)
