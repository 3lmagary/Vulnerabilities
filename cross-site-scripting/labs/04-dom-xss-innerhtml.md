## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** استغلال خاصية `innerHTML` الضعيفة في الجافاسكريبت.
- **الهدف النهائي:** تنفيذ `alert()` باستخدام وسم `<img>`.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** DOM-based XSS (innerHTML Sink)
- **الوصف:** السيرفر يستخدم جافاسكريبت لتحديث محتوى عنصر معين عبر `innerHTML` باستخدام بيانات من الـ URL. خاصية `innerHTML` لا تقوم بتنفيذ وسوم الـ `<script>` ولكنها تسمح بتنفيذ أحداث (Events) عناصر HTML أخرى مثل `onerror`.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** التعرف على الكود المسؤول عن تحديث الصفحة: `document.getElementById('searchMessage').innerHTML = query;`.
2. **الاختبار:** محاولة حقن `<script>` وتلاحظ أنها لن تعمل (سلوك طبيعي لـ innerHTML).
3. **الاستغلال:** استخدام وسم بديل يعتمد على الأحداث:

   ```html
   <img src=x onerror=alert(1)>
   ```

4. **التنفيذ:** المتصفح سيحاول تحميل صورة غير موجودة (`src=x`) وفور الفشل سيقوم بتشغيل حدث الخطأ `onerror`.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser DevTools (Console/Elements)

## 5. لقطات الشاشة (Screenshots)

>
> [!TIP]
> تذكر أن `innerHTML` محمي داخلياً ضد `<script>` ولكن ليس ضد الـ Event Handlers.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Use textContent:** استخدم دائماً `element.textContent` لضمان عرض البيانات كنص وليس كـ HTML.
- **Sanitization:** إذا كان لا بد من استخدام HTML، مرر البيانات عبر مكتبة تنظيف موثوقة أولاً.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-innerhtml-sink)
