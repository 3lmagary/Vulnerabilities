## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** كسر سياق عنصر الـ `<select>` عبر التلاعب ببارامتر البحث.
- **الهدف النهائي:** تنفيذ XSS عبر الخروج من قائمة الاختيارات.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** DOM-based XSS (Select Box Context)
- **الوصف:** التطبيق يستخدم `document.write` لإنشاء قائمة منسدلة (`<select>`) ويعرض الاختيار الحالي بناءً على بارامتر `storeId` في الرابط. الحقن يتم مباشرة داخل وسم `<option>`. لاستغلال هذه الثغرة، يجب إغلاق وسم الاختيار أولاً ثم حقن السكربت.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** ملاحظة الكود المسؤول عن بناء القائمة في صفحة تفاصيل المنتج.
2. **بناء البايلود:** إغلاق التاجات الحالية:

   ```html
   </option><script>alert(1)</script>
   ```

3. **الارسال:** تعديل الرابط ليحتوي على البايلود في بارامتر `storeId`.
4. **التنفيذ:** سيتم تنفيذ السكربت بمجرد تحميل المتصفح للصفحة وبناء القائمة.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (URL manipulation)

## 5. لقطات الشاشة (Screenshots)

>
> [!TIP]
> دائماً افحص هيكلية الـ DOM لتعرف ماهي التاجات التي يجب إغلاقها للوصول لسياق HTML نظيف.

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Avoid document.write:** استخدم الـ DOM API النظامي مثل `createElement` و `appendChild`.
- **Input Sanitization:** التأكد من تنقية أي بيانات تدخل في بناء عناصر الصفحة برمجياً.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-document-write-sink-inside-select-element)
