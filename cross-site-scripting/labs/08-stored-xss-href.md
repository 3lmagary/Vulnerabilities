## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز ترميز علامات التنصيص في سياق الروابط.
- **الهدف النهائي:** حقن كود برمجى في رابط ملف الشخصي للمستخدم.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Stored XSS (href Context)
- **الوصف:** السيرفر يقوم بحماية خاصية الـ `href` عبر ترميز الـ Double Quotes، مما يمنع إضافة خصائص مثل `onclick`. ومع ذلك، المتصفح يدعم تنفيذ جافاسكريبت مباشرة داخل الـ `href` عبر بروتوكول `javascript:`. هذا النوع من الحقن لا يتطلب إغلاق علامات التنصيص أو كسر السياق.

## 3. خطوات الحل (Solution Steps)

1. **التحليل:** إرسال تعليق ووضع رابط عشوائي في خانة "Website".
2. **الاكتشاف:** ملاحظة أن المدخل يظهر داخل `<a href="USER_INPUT">`.
3. **الاستغلال:** وضع البايلود التالي في خانة "Website" عند إرسال التعليق:

   ```
   javascript:alert(1)
   ```

4. **التنفيذ:** سيتم حفظ التعليق؛ وبمجرد ضغط أي مستخدم على اسمك، سيتم تنفيذ السكربت.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Browser (Comment form submission)

## 5. لقطات الشاشة (Screenshots)

>
> [!CAUTION]
> الفلاتر التي تركز على الرموز (`<`, `>`, `"`) غالباً ما تنسى التحقق من نوع البروتوكول (Protocol check).

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **URL Validation:** التحقق من أن الرابط يبدأ ببروتوكولات آمنة فقط (`http`, `https`).
- **Input Filtering:** حظر المدخلات التي تبدأ بـ `javascript:` أو `data:`.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/cross-site-scripting/contexts/lab-href-attribute-double-quotes-html-encoded)
