## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز قيود الوصول المبنية على روابط هوية الـ URL.
- **الهدف النهائي:** الوصول لوحة التحكم وحذف المستخدم `carlos`.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** Access Control Bypass via HTTP Headers
- **الوصف:** الواجهة الأمامية (Forward Proxy/WAF) تحظر الوصول لمسار `/admin`ـ ولكن الخادم الخلفي يدعم رؤوس HTTP مخصصة مثل `X-Original-URL`. يمكن للمهاجم إرسال طلب للمسار الرئيسي `/` مع وضع المسار المحظور في الرأس المذكور، مما يجعل الخادم الخلفي يعالج الطلب وكأنه للمسار المحظور، متجاوزاً بذلك حماية الواجهة الأمامية.

## 3. خطوات الحل (Solution Steps)

1. **الاختبار:** محاولة الوصول لـ `/admin`ـ سيعيد السيرفر `404` أو حظراً.
2. **التجاوز (Bypass):**
    - استخدام **Burp Repeater**.
    - إرسال طلب `GET /`.
    - إضافة رأس: `X-Original-URL: /admin`.
3. **التنفيذ:** بمجرد رؤية لوحة التحكم، قم بتنفيذ الطلب لحذف كارلوس باستخدام نفس التقنية:
    - `GET /?username=carlos` مع رأس `X-Original-Url: /admin/delete`.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Suite (Repeater)
- [x] Header: `X-Original-URL`

## 5. لقطات الشاشة (Screenshots)

![Admin Bypass](../assets/Screenshot-From-2025-10-17-12-52-42.png)

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Unified Rules:** تأكد من أن قواعد الوصول مطبقة في الخادم الخلفي وليس فقط في الواجهات الأمامية.
- **Header Sanitization:** قم بفلترة أو تجاهل أي رؤوس HTTP غير قياسية مثل `X-Original-URL` إذا لم تكن ضرورية للعمل.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/access-control/lab-url-based-access-control-can-be-circumvented)
