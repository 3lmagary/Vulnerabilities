## 1. الهدف من اللابة (Lab Objective)

- **الهدف الرئيسي:** تجاوز حماية الوصول التي تعتمد الصلاحيات فيها على "طريقة HTTP" فقط (GET/POST).
- **الهدف النهائي:** ترقية حساب المستخدم `wiener` إلى Administrator.

## 2. شرح الثغرة (Vulnerability Explanation)

- **نوع الثغرة:** HTTP Method Tampering / Access Control Bypass
- **الوصف:** التطبيق يحمي مسارات الإدارة (مثل ترقية المستخدمين) ويسمح فقط بطلبات `POST` من المدراء. مع ذلك، الخادم الخلفي يقبل تنفيذ نفس الأمر باستخدام طريقة مختلفة (مثل `GET` أو `POST` محولة) دون التحقق من صلاحيات المستخدم في تلك الطرق البديلة.

## 3. خطوات الحل (Solution Steps)

1. **الالتقاط:** تسجيل الدخول كـ `admin` ومراقبة طلب ترقية مستخدم (POST).
2. **التعديل:**
    - إرسال الطلب لـ **Repeater**.
    - تبديل الـ Cookie لتصبح جلسة المستخدم العادي (`wiener`).
    - تغيير طريقة الطلب من `POST` إلى `GET` (أو استخدام `POST` مع تغيير المعاملات).
3. **التنفيذ:** إذا نجح الطلب، سيتم ترقية `wiener` رغم عدم امتلاكه صلاحيات إدارية.

## 4. الأدوات المستخدمة (Tools Used)

- [x] Burp Suite (Repeater, Change Request Method)

## 5. لقطات الشاشة (Screenshots)

![Upgrade User](../assets/Screenshot-From-2025-10-17-16-44-45.png)

---

## 🛡️ كيف تحمي نفسك؟ (Mitigation)

- **Default Deny:** يجب حماية المسار بالكامل (Route) بغض النظر عن الطريقة المستخدمة.
- **Strict Method Checking:** تأكد من أن السيرفر يقبل فقط الطرق المحددة لكل وظيفة ويتحقق من الصلاحيات فيها جميعاً.

---
[رابط المختبر الأصلي](https://portswigger.net/web-security/access-control/lab-method-based-access-control-can-be-circumvented)
